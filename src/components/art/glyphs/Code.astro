---
// Default glyph for `kind: code` notes. 22×22 inline SVG, drawn with
// `currentColor` so it inherits the wrapping `.note-glyph` colour.
---

<svg viewBox="0 0 24 24" width="22" height="22" role="img" aria-label="code glyph">
  <title>code</title>
  <g
    fill="none"
    stroke="currentColor"
    stroke-width="1.4"
    stroke-linecap="round"
    stroke-linejoin="round"
  >
    <path d="M9 7 L 4 12 L 9 17"></path>
    <path d="M15 7 L 20 12 L 15 17"></path>
  </g>
</svg>
