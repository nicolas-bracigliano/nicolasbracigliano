---
// Default vignette for `kind: code` works: the abstract code-editor
// illustration, shared with the home bench via `CodeFigure` so the two
// can't drift. Renders on the dark `.work-art--code` ground; static here
// (the bench adds the cursor blink).
import CodeFigure from '../CodeFigure.astro';
---

<CodeFigure />
