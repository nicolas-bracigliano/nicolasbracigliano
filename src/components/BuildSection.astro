---
export interface Props {
  tag: string;
  title: string;
  /** Tints the row-stack background a faint accent — used to highlight the
   *  "Principles" section, per design system §3. */
  accent?: boolean;
}

const { tag, title, accent = false } = Astro.props;
---

<section class={`build-block${accent ? ' is-accent' : ''}`}>
  <header class="build-block-head">
    <span class="block-tag">§{tag}</span>
    <h2 class="block-title">{title}</h2>
    <span class="block-line" aria-hidden="true"></span>
  </header>
  <dl class="build-rows">
    <slot />
  </dl>
</section>
